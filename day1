class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        out=[nums[0]]
        for i in range(1,len(nums)):
            nums[i]+=nums[i-1]
            out.append(nums[i])
        return out
Console
